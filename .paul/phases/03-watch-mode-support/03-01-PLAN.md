---
phase: 03-watch-mode-support
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - .github/workflows/ci.yml
  - src/verify.ts
  - src/types.ts
  - README.md
  - test/watch-mode.test.ts
autonomous: true
---

<objective>
## Goal
Implement watch mode detection for test runners (Jest, Vitest) and enhance terminal output with colored formatting using chalk.

## Purpose

Enable the verify command to detect when test runners are running in watch mode, handle long-running processes appropriately, and provide colored terminal output for better readability.

## Output

- Chalk dependency added for colored terminal output
- Watch mode detection in verify.ts for Jest and Vitest
- Colored output formatting for test results
- GitHub Actions CI workflow updated with watch mode tests
- README.md updated with watch mode documentation
- Unit tests for watch mode detection
  </objective>

<context>
## Project Context
.paul/PROJECT.md
.paul/ROADMAP.md
.paul/STATE.md

## Source Files

src/verify.ts — Verification runner with runCheck and parseTestOutput functions
src/types.ts — Type definitions for CheckResult, VerifyResult, ProgressUpdate
src/index.ts — Command handler and result formatting
.github/workflows/ci.yml — Existing GitHub Actions workflow
README.md — Project documentation

## Current State

- Test output formatting completed with ✓/✗ markers
- Progress callbacks implemented for real-time updates
- Test parser supports Jest, Vitest, and node:test
- No colored output (ASCII only)
- No watch mode detection
  </context>

<skills>
## Required Skills
No specialized skills required for this plan.
</skills>

<acceptance_criteria>

## AC-1: Chalk Dependency Added

Given the project uses pnpm
When chalk is added as a dependency
Then package.json includes "chalk" in dependencies
And pnpm-lock.yaml is updated
And the project still passes typecheck

## AC-2: Watch Mode Detection

Given a test runner running in watch mode
When verify detects watch mode flags (--watch, --watchAll)
Then it identifies the watch mode status
And returns a flag in the CheckResult indicating watch mode

## AC-3: Colored Terminal Output

Given chalk is installed
When formatting test results
Then pass indicators are shown in green
And fail indicators are shown in red
And warnings are shown in yellow

## AC-4: GitHub Actions CI Updated

Given the CI workflow exists
When updated for Phase 3
Then it includes a step to test watch mode detection
And all existing checks still pass

## AC-5: README.md Documentation

Given watch mode is implemented
When README.md is updated
Then it includes a section explaining watch mode detection
And documents the new colored output feature

## AC-6: Watch Mode Tests

Given watch mode detection is implemented
When test files are added
Then tests verify watch mode flag detection for Jest and Vitest
And all tests pass
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add chalk dependency for colored output</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
    Run: pnpm add chalk
    This will update package.json and pnpm-lock.yaml
    Verify chalk appears in dependencies (not devDependencies)
    Ensure typecheck still passes after adding chalk
  </action>
  <verify>grep -E '"chalk"' package.json && pnpm run typecheck</verify>
  <done>AC-1 satisfied: Chalk dependency added and project builds</done>
</task>

<task type="auto">
  <name>Task 2: Implement watch mode detection in src/verify.ts</name>
  <files>src/verify.ts, src/types.ts</files>
  <action>
    In src/types.ts:
    - Add watchMode?: boolean to CheckResult interface
    
    In src/verify.ts:
    - Add detectWatchMode(command: string, output: string): boolean function
    - Detect watch mode flags in command: --watch, --watchAll, --watch=all
    - Parse output for watch mode indicators ("Watch Usage", "press w to show more")
    - Set watchMode flag in CheckResult
    - Update runCheck to detect watch mode from both command and output
  </action>
  <verify>grep -E 'watchMode|detectWatchMode' src/verify.ts src/types.ts | head -5</verify>
  <done>AC-2 satisfied: Watch mode detection implemented</done>
</task>

<task type="auto">
  <name>Task 3: Add colored output formatting using chalk</name>
  <files>src/verify.ts, src/index.ts</files>
  <action>
    In src/verify.ts:
    - Import chalk from 'chalk'
    - Create formatWithColor(text: string, status: 'success' | 'error' | 'warning'): string
    
    In src/index.ts:
    - Import chalk
    - Update formatCheckResult to use chalk.green() for ✓, chalk.red() for ✗
    - Use chalk.yellow() for warnings/skipped tests
    - Keep fallback to plain text if chalk is not available (for non-TTY environments)
  </action>
  <verify>grep -E "import chalk|chalk\.(green|red|yellow)" src/verify.ts src/index.ts | head -5</verify>
  <done>AC-3 satisfied: Colored terminal output implemented</done>
</task>

<task type="auto">
  <name>Task 4: Update .github/workflows/ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
    Add a new step "Test watch mode detection" after existing test steps:
    - Run a test that verifies watch mode detection logic
    - Use a mock or simulated test runner output
    - Ensure step reports individually in GitHub Actions
    Keep all existing steps (typecheck, smoke-test, test, lint, format:check)
  </action>
  <verify>grep -E "watch mode|Watch Mode" .github/workflows/ci.yml</verify>
  <done>AC-4 satisfied: CI workflow includes watch mode tests</done>
</task>

<task type="auto">
  <name>Task 5: Create test files for watch mode feature</name>
  <files>test/watch-mode.test.ts</files>
  <action>
    Create test/watch-mode.test.ts with tests for:
    - detectWatchMode identifies --watch flag in command string
    - detectWatchMode identifies --watchAll flag
    - detectWatchMode parses "Watch Usage" in output
    - CheckResult includes watchMode flag when detected
    - Tests pass with pnpm run test
  </action>
  <verify>cat test/watch-mode.test.ts | head -10 && pnpm run test -- test/watch-mode.test.ts</verify>
  <done>AC-6 satisfied: Watch mode tests created and passing</done>
</task>

<task type="auto">
  <name>Task 6: Update README.md with watch mode documentation</name>
  <files>README.md</files>
  <action>
    Add a "Features" or "Watch Mode Support" section that documents:
    - Automatic watch mode detection for Jest and Vitest
    - How the verify command handles watch mode (graceful exit, status indication)
    - Colored terminal output feature
    - Example output showing colored results
    
    Ensure documentation is consistent with existing style.
  </action>
  <verify>grep -E "Watch Mode|watch mode|colored output|chalk" README.md | head -5</verify>
  <done>AC-5 satisfied: README.md updated with watch mode documentation</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE
- Existing test logic in test/commands.test.ts, test/tool.test.ts
- Core verification execution flow (runVerification, runCheck signatures)
- Extension API compatibility (pi.registerCommand, pi.registerTool)

## SCOPE LIMITS

- Watch mode detection only for Jest and Vitest (not node:test --watch)
- Chalk colors only in terminal output (not in JSON/tool results)
- CI workflow should not actually run tests in watch mode (just verify detection logic)
- Colored output gracefully degrades to plain text in non-TTY environments
  </boundaries>

<verification>
Before declaring plan complete:
- [ ] pnpm run typecheck passes
- [ ] pnpm run lint passes
- [ ] pnpm run test passes (including new watch-mode.test.ts)
- [ ] pnpm run check passes
- [ ] Chalk is listed in package.json dependencies
- [ ] Watch mode detection works for Jest and Vitest patterns
- [ ] Colored output visible in terminal (manual check)
- [ ] README.md updated with watch mode section
</verification>

<success_criteria>

- All 6 tasks completed
- All acceptance criteria (AC-1 through AC-6) satisfied
- TypeScript strict mode compliance
- No linting errors
- All tests pass (18 existing + new watch mode tests)
- Plan executes autonomously (no checkpoints triggered)
  </success_criteria>

<output>
After completion, create .paul/phases/03-watch-mode-support/03-01-SUMMARY.md
</output>
