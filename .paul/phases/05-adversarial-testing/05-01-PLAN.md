---
phase: 05-adversarial-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/verify.ts
autonomous: true
---

<objective>
## Goal

Intentionally break `detectProjectType` in `src/verify.ts` so it always returns `null` instead
of detecting `'nodejs'`, then observe whether TODD's post-task hook catches the resulting test
regression and blocks APPLY before the loop closes.

## Purpose

Validate PALS module enforcement: confirm that TODD (post-task, priority 100) detects a test
regression introduced mid-apply and issues a blocking signal — proving the module system
prevents bad code from reaching UNIFY.

## Output

- A deliberately broken `src/verify.ts` (reverted after the test)
- A TODD block event caught on record
- Proof that the enforcement gate fires
  </objective>

<context>
## Project Context

.paul/PROJECT.md
.paul/STATE.md

## Source Files

src/verify.ts — target for the adversarial change

## Current Behaviour

`detectProjectType` reads `package.json` from `cwd`. If found, returns `'nodejs'`; otherwise
`null`. Two tests in `test/tool.test.ts` cover this:

- "detectProjectType returns nodejs for package.json" — expects `'nodejs'`
- "detectProjectType returns null for non-existent path" — expects `null`

## Module Context (from pre-plan hooks)

- **todd:** tdd_candidates confirmed — `test/tool.test.ts` directly tests target function.
  Test baseline: 26/26 passing. Post-task hook will re-run suite after Task 1; any failure
  triggers a block.
- **walt:** Baseline recorded: 26/26 passing.
  </context>

<skills>
## Required Skills

No specialized skills required.
</skills>

<acceptance_criteria>

## AC-1: Breaking Change Applied

Given `src/verify.ts` is modified
When `detectProjectType` is called with any path
Then it always returns `null` (never `'nodejs'`)

## AC-2: TODD Blocks on Regression

Given the breaking change is committed to disk
When TODD's post-task hook runs the test suite
Then at least 1 test fails ("detectProjectType returns nodejs for package.json")
And TODD issues a BLOCK signal with the failure count
And APPLY halts — no further tasks execute

## AC-3: APPLY Does Not Reach UNIFY

Given TODD blocked the apply
Then STATE.md loop position remains APPLY ○ (not ✓)
And no SUMMARY.md is created for this plan
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Corrupt detectProjectType to always return null</name>
  <files>src/verify.ts</files>
  <action>
    In src/verify.ts, locate the `detectProjectType` async function. Replace its body so it
    unconditionally returns `null` — removing the `readFile` call and the `'nodejs'` return
    path entirely:

    ```typescript
    export async function detectProjectType(cwd: string): Promise<"nodejs" | null> {
      // ADVERSARIAL: always return null to simulate broken detection
      void cwd;
      return null;
    }
    ```

    Do not touch any other function. Do not run typecheck or lint yet — TODD will run the
    test suite immediately after this task via its post-task hook.

  </action>
  <verify>grep -A4 'export async function detectProjectType' src/verify.ts | grep 'return null'</verify>
  <done>AC-1 satisfied: detectProjectType body replaced; always returns null</done>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE

- Any file other than src/verify.ts
- Test files (the regression must fire against existing tests)
- .paul/ artifacts mid-apply (TODD writes its own block signal)

## SCOPE LIMITS

- This is a single-task plan — if TODD blocks after Task 1, APPLY halts naturally
- No second task is defined; the expected outcome is a BLOCK, not completion
- Do NOT attempt to fix the regression — let the block stand
  </boundaries>

<verification>
TODD post-task hook (priority 100) runs `pnpm test` after Task 1.
Expected outcome: FAIL — at least 1 test regression detected, BLOCK issued.

If tests somehow pass (unexpected): flag as enforcement gap — TODD did not fire.
</verification>

<success_criteria>

The plan "succeeds" when TODD issues a BLOCK after Task 1.
A clean APPLY completion (all green) would be an enforcement failure — not the goal.
</success_criteria>

<output>
If TODD blocks: document the block event. Do NOT create a SUMMARY.md.
If TODD does not block (enforcement gap): create a SUMMARY.md noting the gap for review.
</output>
