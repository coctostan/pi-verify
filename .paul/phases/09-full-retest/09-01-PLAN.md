---
phase: 09-full-retest
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/verify.ts
  - src/index.ts
  - src/tool.ts
  - src/constants.ts
  - .paul/STATE.md
autonomous: true
---

<objective>
## Goal

Hardened retest of two-step dispatch with upgraded advisory output — validate
IRIS/RUBY/DOCS/SKIP run first with enhanced visibility, then WALT enforcement block.

## Purpose

Confirm that the two-step dispatch pattern works reliably with varied inputs and
upgraded advisory module output formatting.

## Output

Demonstration of hardened two-step dispatch with all 5 advisory triggers firing
before WALT lint block.
</objective>

<context>
## Source Files

src/verify.ts — add complex function (triggers RUBY/IRIS)
src/index.ts — add unused import (triggers WALT block)
src/tool.ts — add constant without README/CHANGELOG update (triggers DOCS drift)
src/constants.ts — add fake API key comment (triggers IRIS security)
.paul/STATE.md — record decision (triggers SKIP extraction)

## Hardened Two-Step Dispatch Pattern

STEP 1: advisory_module_dispatch (upgraded output)

- IRIS: code smells, security patterns with enhanced formatting
- RUBY: debt flags with severity indicators
- DOCS: drift detection with file catalog
- SKIP: knowledge extraction with suggested actions

STEP 2: enforcement_module_dispatch

- WALT: lint/type checks → BLOCK if violations

Expected: Advisory output visible with upgraded formatting before block.
</context>

<acceptance_criteria>

## AC-1: Hardened Advisory Triggers

Given all 5 advisory triggers
When STEP 1 runs
Then IRIS, RUBY, DOCS, SKIP all fire with detections:

- IRIS: 6 params, 60 lines, 4 nesting, magic 42, fake API key
- RUBY: complexity, LOC, parameters, magic number
- DOCS: tool.ts changed, README/CHANGELOG not updated
- SKIP: decision extracted from STATE.md

## AC-2: WALT Block After Advisory

Given STEP 2 runs after STEP 1 completes
When WALT detects lint violations
Then BLOCK is issued with advisory output already visible

## AC-3: Hardened Two-Step Semantics

Given hardened test conditions
When dispatch runs
Then user sees: [upgraded advisory output] → [enforcement block]
</acceptance_criteria>

<tasks>
<task type="auto">
  <name>Task 1: Add hardened complex function (RUBY/IRIS)</name>
  <files>src/verify.ts</files>
  <action>
    Add hardenedFullRetestFunction() with 6 parameters, 60+ lines,
    4 levels of nesting, and magic number 42. Tests RUBY debt and
    IRIS smell detection with hardened conditions.
  </action>
  <verify>grep -c 'hardenedFullRetestFunction' src/verify.ts</verify>
</task>

<task type="auto">
  <name>Task 2: Add unused import for WALT block</name>
  <files>src/index.ts</files>
  <action>
    Add import { existsSync } from "fs"; as unused import.
    Triggers WALT lint block for hardened retest.
  </action>
  <verify>grep 'existsSync' src/index.ts</verify>
</task>

<task type="auto">
  <name>Task 3: Change tool.ts without doc updates (DOCS drift)</name>
  <files>src/tool.ts</files>
  <action>
    Add const RETEST_MARKER = "v0.7"; to tool.ts.
    Deliberately skip README.md and CHANGELOG.md updates.
  </action>
  <verify>grep 'RETEST_MARKER' src/tool.ts</verify>
</task>

<task type="auto">
  <name>Task 4: Record hardened decision (SKIP extraction)</name>
  <files>.paul/STATE.md</files>
  <action>
    Add decision: "Chose ESLint over custom AST analysis for complexity detection"
    to the Decisions table for hardened SKIP test.
  </action>
  <verify>grep 'ESLint over custom AST analysis' .paul/STATE.md</verify>
</task>

<task type="auto">
  <name>Task 5: Add fake API key comment (IRIS security)</name>
  <files>src/constants.ts</files>
  <action>
    Add comment with fake API key: // sk-test-fake123
    Tests IRIS security pattern detection on comment content.
  </action>
  <verify>grep 'sk-test-fake123' src/constants.ts</verify>
</task>
</tasks>

<boundaries>
## DO NOT CHANGE

- README.md (keep drift condition)
- CHANGELOG.md (keep drift condition)
- Any fix to remove unused import (let WALT block)

## SCOPE LIMITS

Hardened retest validates two-step dispatch reliability.
If dispatch fails, document as hardened test gap.
</boundaries>

<verification>
Hardened two-step dispatch result check:
- Advisory output (IRIS, RUBY, DOCS, SKIP) visible with upgraded formatting: YES/NO
- Enforcement block (WALT) after advisory with context: YES/NO
- All 5 triggers detected: YES/NO
</verification>

<success_criteria>
Hardened retest succeeds if two-step dispatch works with all triggers
OR if gaps are documented for future hardening.
</success_criteria>

<output>
SUMMARY.md documenting hardened retest results.
</output>
