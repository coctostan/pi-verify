---
phase: 10-cleanup-identity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "README.md", "src/verify.ts", "src/index.ts", "src/tool.ts"]
autonomous: true
---

<objective>
## Goal
Transform the template-based extension into a polished, ship-ready product with correct package identity, cleaned codebase, and professional documentation.

## Purpose

Remove testing artifacts and template remnants to prepare pi-verify for v1.0.0 publication. This establishes production-ready identity and eliminates technical debt accumulated during iterative development.

## Output

- Package renamed to `pi-verify` at v1.0.0
- All complexity bait functions removed
- Formatter utilities consolidated
- README rewritten as product documentation
  </objective>

<context>
## Project Context
- .paul/PROJECT.md — Core value and scope
- .paul/ROADMAP.md — v1.0 milestone phases
- .paul/PRD.md — Product requirements

## Source Files

- package.json — Template identity (my-pi-extension v0.1.0)
- README.md — Template documentation
- src/verify.ts — Contains 3 complexity bait test functions (lines 455-693)
- src/index.ts — Contains formatCheckResult (duplicated in tool.ts)
- src/tool.ts — Contains formatCheckResult (duplicated in index.ts)
  </context>

<acceptance_criteria>

## AC-1: Package Identity Correct

```gherkin
Given the package.json file
When I read the name, version, description, and keywords fields
Then name is "pi-verify"
And version is "1.0.0"
And description mentions "verification" or "verify"
And keywords include "pi-verify" (not "template")
And pi.image is a valid URL or removed
```

## AC-2: Complexity Functions Removed

```gherkin
Given src/verify.ts
When I search for complexity bait functions
Then processVerificationComplex is not present
And twoStepDispatchTestFunction is not present
And hardenedFullRetestFunction is not present
And all "ADVISORIAL" and "TEST:" comments are removed
And file line count is reduced significantly
```

## AC-3: Formatters Deduplicated

```gherkin
Given src/index.ts and src/tool.ts
When I examine formatting functions
Then only one formatCheckResult implementation exists
And the consolidated version is exported from a single location
And all imports reference the consolidated location
```

## AC-4: README Rewritten as Product Docs

```gherkin
Given README.md
When I read the content
Then it does not mention "starter template" or "Use this template"
Then it describes pi-verify functionality for end users
And installation instructions use "pi install" not GitHub template flow
And the /verify command is documented with examples
And the verify_check tool is documented
```

## AC-5: All Checks Pass

```gherkin
Given the modified codebase
When I run "pnpm run check"
Then typecheck passes
And tests pass
And lint passes
And format check passes
```

</acceptance_criteria>

<tasks>

<task type="auto">
  <name>Task 1: Fix Package Identity in package.json</name>
  <files>package.json</files>
  <action>
    Update package.json fields to reflect pi-verify product identity:
    
    1. Change "name" from "my-pi-extension" to "pi-verify"
    2. Change "version" from "0.1.0" to "1.0.0"
    3. Change "description" to: "Pi extension for running verification checks (typecheck, test, lint, build) with structured output"
    4. Update "keywords" to: ["pi-package", "pi-extension", "verify", "test", "lint", "typecheck"]
    5. Update "pi.image" to a meaningful value or remove placeholder
    6. Remove "setup-template" script (not needed for published package)
    
    Keep all other scripts and dependencies intact.
  </action>
  <verify>cat package.json | grep -E '"name"|"version"|"description"' | head -5</verify>
  <done>AC-1 satisfied: Package identity fields updated correctly</done>
</task>

<task type="auto">
  <name>Task 2: Remove Complexity Bait Functions from verify.ts</name>
  <files>src/verify.ts</files>
  <action>
    Remove the following test-only complexity bait functions and their comments:
    
    1. Delete processVerificationComplex function (lines ~455-531)
    2. Delete twoStepDispatchTestFunction function (lines ~537-609)
    3. Delete hardenedFullRetestFunction function (lines ~615-693)
    
    Also remove related ADVISORIAL comments:
    - Line ~14: "ADVISORIAL: Hardcoded API key for IRIS security detection testing"
    - Line ~51: "ADVISORIAL: Added engines field check — tests DOCS drift detection"
    
    These were intentionally complex functions used for testing PALS module detection.
    They serve no production purpose and should be completely removed.
    
    Verify the file still exports all production functions:
    - detectProjectType
    - getCheckCommand
    - parseTestOutput
    - runCheck
    - runVerification
    - detectWatchMode
    - supportsWatchMode
    - categorizeError
    - getSuggestedFix
    - aggregateErrors
  </action>
  <verify>grep -E "processVerificationComplex|twoStepDispatchTestFunction|hardenedFullRetestFunction" src/verify.ts || echo "Functions removed"</verify>
  <done>AC-2 satisfied: All complexity bait functions removed</done>
</task>

<task type="auto">
  <name>Task 3: Consolidate formatCheckResult Implementation</name>
  <files>src/index.ts, src/tool.ts, src/formatters.ts (new)</files>
  <action>
    Deduplicate formatCheckResult by creating a shared formatter utility:
    
    1. Create new file src/formatters.ts with the consolidated formatCheckResult function
       - Export the version that accepts optional chalkInstance parameter (more flexible)
       - Include proper JSDoc comments
       - Import necessary types from ./types.js
    
    2. Update src/index.ts:
       - Remove local formatCheckResult function definition
       - Import formatCheckResult from ./formatters.js
       - Keep formatVerifyResult and formatErrorSummary in index.ts (they use chalk directly)
    
    3. Update src/tool.ts:
       - Remove local formatCheckResult function definition
       - Import formatCheckResult from ./formatters.js
    
    The consolidated function signature should be:
    ```typescript
    export function formatCheckResult(
      check: CheckResult,
      chalkInstance?: typeof chalk
    ): string
    ```
  </action>
  <verify>grep -c "formatCheckResult" src/index.ts src/tool.ts src/formatters.ts 2>/dev/null | grep -v ":0$" | wc -l</verify>
  <done>AC-3 satisfied: formatCheckResult deduplicated into shared formatters module</done>
</task>

<task type="auto">
  <name>Task 4: Rewrite README.md as Product Documentation</name>
  <files>README.md</files>
  <action>
    Rewrite README.md to focus on pi-verify as a shipped product:
    
    Structure:
    1. **Header**: # pi-verify — with one-line description
    2. **What it does**: Brief description of verification functionality
    3. **Installation**: `pi install npm:pi-verify` (for users installing from npm)
    4. **Commands**: Document /verify with all subcommands (all, test, lint, quick, help)
    5. **Tool**: Document verify_check tool for model-callable use
    6. **Features**: Error categorization, watch mode detection, colored output
    7. **Requirements**: Node.js projects with package.json
    8. **License**: MIT
    
    Remove all template-specific content:
    - "Use this template" on GitHub
    - "starter template" language
    - setup-template script references
    - starters/ directory documentation
    - Instructions for extension developers (this is for end users now)
    
    Keep sections that describe actual functionality:
    - Watch mode support details
    - Error summarization details
    - Structured output format
  </action>
  <verify>grep -c "template\|starter\|Use this" README.md || echo "0 (template references removed)"</verify>
  <done>AC-4 satisfied: README rewritten as product documentation</done>
</task>

<task type="auto">
  <name>Task 5: Verify All Checks Pass</name>
  <files>package.json, src/verify.ts, src/index.ts, src/tool.ts, src/formatters.ts, README.md</files>
  <action>
    Run the full verification check to ensure no regressions:
    
    1. Run `pnpm install` if needed to update lockfile
    2. Run `pnpm run typecheck` — verify no TypeScript errors
    3. Run `pnpm run test` — verify all tests pass
    4. Run `pnpm run lint` — verify no ESLint errors
    5. Run `pnpm run format:check` — verify Prettier formatting
    
    Fix any issues that arise from the changes:
    - Missing imports in new formatters.ts
    - Export/import mismatches
    - Type errors from removed functions
  </action>
  <verify>pnpm run check</verify>
  <done>AC-5 satisfied: All verification checks pass</done>
</task>

</tasks>

<boundaries>

## DO NOT CHANGE

- src/constants.ts — Extension constants (names already set correctly)
- src/types.ts — Type definitions (stable)
- src/commands.ts — Command parsing utilities (stable)
- test/ directory — Test files should continue to work
- tsconfig.json, eslint.config.js, .prettierrc — Tooling configs stable

## SCOPE LIMITS

- Do not add new features (this is cleanup only)
- Do not modify verification logic behavior
- Do not change tool or command registration
- Do not alter error categorization logic
- Focus is identity, cleanup, and documentation only

</boundaries>

<verification>
Before declaring plan complete:
- [ ] package.json shows name "pi-verify" and version "1.0.0"
- [ ] grep for complexity functions returns no results
- [ ] src/formatters.ts exists with consolidated formatCheckResult
- [ ] README.md has no "template" or "starter" references
- [ ] `pnpm run check` passes all stages
- [ ] All acceptance criteria met
</verification>

<success_criteria>

- Package identity updated to pi-verify v1.0.0
- 3 complexity bait functions (~240 lines) removed from verify.ts
- formatCheckResult deduplicated into shared formatters module
- README rewritten as user-facing product documentation
- All existing tests pass
- No new lint/type errors introduced
  </success_criteria>

<output>
After completion, create `.paul/phases/10-cleanup-identity/10-01-SUMMARY.md`
</output>
